const User = require('../models/user');
const catchCreateClassmate = fn => { return async (req, res, next) => {
//   const data = req.body;
//   const duplicateUser = await User.findOne({where : {email : data.email}});
//   if(!data.firstName || !data.lastName || !data.email || !data.imageUrl || !data.subheading || !data.password || !data.passwordConfirm)
//       throw new Error('Empty Field');
//   else if(duplicateUser)
//       throw new Error('Duplicate User');
//   fn(req, res, next).catch(next); 

}}
module.exports = catchCreateClassmate;


